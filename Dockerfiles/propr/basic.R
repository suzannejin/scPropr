# data.table to manage large datasets
pkgs = c("R.utils",
         "data.table",
         "GGally",
         "ggpubr",
         "argparse",
         "optparse",
         'this.path',
         'here',
         'import'
         )
install.packages(pkgs)