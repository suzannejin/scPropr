# normalization packages
BiocManager::install("edgeR")  # for TMM normalization
BiocManager::install("scran")  # for single cell scran normalization


# entropy pseudocount calculation
install.packages('entropy')